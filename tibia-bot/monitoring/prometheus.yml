global:
  scrape_interval: 5s
  evaluation_interval: 15s

scrape_configs:
  - job_name: tibia-bot
    metrics_path: /metrics
    scrape_interval: 5s
    static_configs:
      - targets:
          - host.docker.internal:8080
        labels:
          instance: bot
          job: tibia-bot
